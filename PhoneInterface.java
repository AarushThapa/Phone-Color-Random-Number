

public interface PhoneInterface
{
    // call function to make the call
    void call(String phoneNumber);
    // hangout function to end the call
    void hangup();
    // clear function to clear the text field
    void clear();
}
